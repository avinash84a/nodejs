'use strict';

function assembleStyles () {
	
		var thor = {

			run :(app)=>{
				console.log(app.config); 
				app.listen(app.config.port,()=>{
				  console.log("listning "+app.config.port);
				});
				console.log ( "we are in run "); 
			}

		}; 


		return thor ;
		
}

Object.defineProperty(module, 'exports', {
	enumerable: true,
	get: assembleStyles
});
